# peer 依赖：自动补齐，但不因不匹配而中断安装
# 设为 true 会让 Tauri/NestJS 生态的 peer 警告变成硬失败，单人项目得不偿失
strict-peer-dependencies=false
auto-install-peers=true

# 依赖版本解析取满足范围的最高版本
resolution-mode=highest

# CI 与本地都要求 lockfile 与 package.json 一致，防止意外升级
prefer-frozen-lockfile=true

# 减少安装日志噪声
loglevel=warn
