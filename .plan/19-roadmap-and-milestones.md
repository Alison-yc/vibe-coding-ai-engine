# 19 · 路线图、里程碑与简历产出物

| 项   | 值                   |
| ---- | -------------------- |
| 阶段 | 全局                 |
| 依赖 | 00～18               |
| 状态 | 持续维护（执行基准） |

## 排期总览

时间估算按**每周投入 20～25 小时**（业余时间）计算。如果是全职投入，除以 2.5。

| 里程碑 | 内容                         | 工期   | 累计    |
| ------ | ---------------------------- | ------ | ------- |
| M0     | 工程底座                     | 2 周   | 2 周    |
| M1     | 模型基线 + 数据层            | 1.5 周 | 3.5 周  |
| M2     | RAG + 对话页                 | 3 周   | 6.5 周  |
| M3     | 工作流引擎 + 画布            | 3.5 周 | 10 周   |
| M4     | Agent 工具 + MCP             | 3 周   | 13 周   |
| M5     | 双端 + dmg 打包              | 1.5 周 | 14.5 周 |
| 收尾   | 评测、文档、README、演示视频 | 1 周   | 15.5 周 |

**约 4 个月。** 这是诚实的估算，包含了踩坑和返工的时间。看到"两周做完一个 Dify"的说法不要信。

## 怎么决定下一步：开发还是 Review

plan 编号表示**主题**，不是严格的执行队列。实际执行顺序以本文件的 CR 批次为准。

每次开始工作前按下面顺序判断：

1. `.plan/README.md` 的「当前执行点」是否标记为 `待开发`：只开发该 CR 批次，不顺手进入下一个批次。
2. 当前批次实现完成但尚未通过自身 DoD：继续开发或修复。
3. 当前批次 DoD 全部满足且状态为 `待 CR`：停止新增功能，执行代码审查。
4. CR 有阻塞问题：只修 review 发现的问题，然后重新 CR。
5. CR 通过：更新当前批次为 `已完成`，再进入下一批次。
6. 一个里程碑内的所有 CR 都完成：先做「里程碑集成 Review」，通过后才能进入下一个里程碑。

**禁止连续完成多个 CR 批次后统一 review。** 底层错误会扩散到后续模块，最终很难判断是哪一个批次引入的。

### 状态定义

| 状态        | 含义                   | 模型下一步        |
| ----------- | ---------------------- | ----------------- |
| `未开始`    | 前置 CR 尚未完成       | 不得开工          |
| `待开发`    | 前置满足，当前应执行   | 按批次范围开发    |
| `开发中`    | 实现或验证尚未完成     | 继续本批次        |
| `待 CR`     | DoD 已满足，尚未审查   | 停止开发，执行 CR |
| `CR 修复中` | review 有阻塞问题      | 只修 review 问题  |
| `已完成`    | CR 已通过              | 进入下一批次      |
| `已搁置`    | 主动取消，并已记录原因 | 跳过              |

### 三层 Review

1. **批次 CR**：每个 CR 批次完成后执行，检查代码、测试、规则和该批次 DoD。
2. **里程碑集成 Review**：M0～M5 结束时执行，检查跨模块链路和里程碑 DoD；不能替代批次 CR。
3. **专项门禁 Review**：`04`、`08` 核对实测与 ADR；`13-A` 核对可安装交付物。通过后再决定后续路线。

## 实施批次与 CR 门禁

一个 CR 通常控制在 1～3 天。可以包含同一目标下的多个 plan 子阶段，但必须拆成清晰提交，且有共同的验收边界。

### M0 · 工程底座

#### CR-01 · Monorepo 与测试运行器底座

- **包含**：`02` 全部；`15-A` 的 Vitest projects、基础 coverage、NestJS 测试框架实测。
- **不包含**：FakeLlmGateway、LLM 测试生成、变异测试、完整 E2E。
- **Review 重点**：workspace 边界、任务依赖、架构护栏、格式与测试命令是否真的会失败。
- **通过条件**：`02` DoD；最小 `pnpm test` / `typecheck` / `lint` 可运行；ADR-D03 有实测结论。

#### CR-02 · 契约层与平台接口地基

- **包含**：`03`；`12-A`（platform + 最小 Web/Tauri 壳）。
- **提交边界**：契约层与 platform 层分成两个提交。
- **Review 重点**：跨进程类型唯一来源、运行时校验、platform 是否泄漏 Tauri/Web 形状、两个壳是否只做装配。
- **通过条件**：`03` DoD；`12-A` 的接口、最小双端壳、Provider 与 ESLint 护栏完成；`pnpm dev:web` 可用。

#### CR-03 · 设计令牌底座

- **包含**：`14-A`；`15-C` 的 Playwright 配置与第一条主题/令牌页 smoke。
- **不包含**：为未来页面预造大量业务组件。
- **Review 重点**：语义令牌、跨包样式是否丢失、UI 包依赖洁癖。

#### CR-04 · 安全与 CI 门禁骨架

- **包含**：`16-A` 的规则自测/SCA/Gitleaks；`17-A` 的快速门禁、测试、构建、安全 job。
- **依赖**：CR-01～03。
- **Review 重点**：故意制造错误时门禁是否真的失败；CI 与本地命令是否一致。
- **不要求**：尚未存在功能的攻击测试；它们随 `06` / `08` / `10` 补。

#### M0 集成 Review

- `pnpm ci:local` 全绿。
- 六个包的依赖方向正确。
- contracts、platform、ui、app-core 的架构护栏实测有效。
- CI 在远端真实运行，不能只做 YAML 静态检查。
- 通过后才能进入 M1。

本地自检（2026-08-26）：`packages/` 六包依赖方向符合分层（contracts 仅 zod；tsconfig 无运行时依赖；platform/ui 不依赖业务包；app-core 依赖 contracts + platform + ui）。护栏由根 `eslint.config.js` 与 `packages/eslint-config/src/architecture-guards.spec.js` 实测。workflow 已扩大到 `feature/**` / `feat/**` / `fix/**` 并支持 `workflow_dispatch`。

阻塞修复：type-aware ESLint 必须在 workspace 包 `dist` 存在之后运行。干净 checkout 上先 lint 会把未解析的 `@ai-engine/*` 打成 error type（约 116 条 `no-unsafe-*`）。CI 与 `pnpm lint` 改为先 `typecheck`（`^build` 产出 `dist`）再 eslint。远端是否整条绿：push 后看 GitHub Actions。

### M1 · 模型能力与数据层

#### CR-05 · 模型基线与 LLM 网关（决策门禁）

- **包含**：`04` 全部，含 FakeLlmGateway。
- **必须单独 CR**：不能与数据库或 RAG 合并审查。
- **Review 重点**：基线报告能否支撑 `numCtx`、`numPredict`、超时、工具数量等代码参数。
- **决策**：完成 ADR-D01；同步修订 `08` / `10` 中被实测推翻的假设。

#### CR-06 · PostgreSQL、pgvector 与 Drizzle

- **包含**：`05`；`15-B` 的事务集成测试辅助；`17-B` 的 Postgres CI 集成。
- **Review 重点**：迁移可重建、向量维度唯一来源、参数化 SQL、数据持久化、CI 数据库不依赖开发机状态。

#### CR-07 · 可观测性基础

- **包含**：`18-A`（步骤 1～6）：日志、traceId、LLM 指标、环形缓冲、调试面板、ErrorBoundary。
- **不包含**：RAG 评测数据集和优化实验。
- **Review 重点**：日志无用户全文/文件内容；一次请求能用 traceId 串联。

#### M1 集成 Review

- 真实 Ollama、FakeLlmGateway、数据库三者边界清晰。
- 模型参数全部有报告出处。
- 空库能通过迁移完整重建。
- 复盘基线数据是否改变 M2～M4 的设计。

已通过（2026-08-27）。未推翻 M2～M4 主方案：Agent 维持 ADR-D01 路线 A（`maxToolCount=6`）；结构化输出必须用 Ollama JSON Schema；默认 `numCtx=8192`。`InMemoryVectorStore` 仅作无库/单测回退。本机空 volume 重建未当面验证，不阻断进入 M2。

### M2 · RAG 与对话

#### CR-08 · RAG 索引与检索

- **包含**：`06`；对应的契约、测试和提示词注入安全用例。
- **Review 重点**：五阶段可重试、检索不经过 LLM 也能验证、外部文档被视为不可信数据。

#### CR-09 · RAG 评测基线

- **包含**：`18-B`（步骤 7～11）：评测集、Recall@k、MRR、拒答/注入指标、首份报告。
- **Review 重点**：指标可复现，报告带配置快照；必要时返回 CR-08 修检索。

#### CR-10 · 对话助手垂直链路

- **包含**：`07`；业务所需的 `14-B` 增量组件；对应的 `15` 测试与 `16` XSS 用例。
- **Review 重点**：会话 CRUD、SSE 终止/取消、event reducer、落库顺序、引用溯源。

#### CR-10A · LLM 测试生成闭环

- **包含**：`15-D`，选择 M2 中一个真实低覆盖率文件跑通上下文包、生成验证与变异测试。
- **Review 重点**：生成测试是否真的能因实现损坏而失败；未经人工审核的文件不得转为正式测试。
- **不包含**：为了展示流水线而创建假业务文件或降低覆盖率阈值。

#### M2 集成 Review

- 完整走通「上传文档 → 索引 → 检索 → 对话 → 引用」。
- 评测报告与当前实现参数一致。
- LLM 测试生成闭环已在真实文件上验证。
- 录制第一个可演示版本后再进入 M3。

集成 Review 已通过（2026-08-27）。代码与测试门禁完成；本机已跑通带 Ollama/Postgres 的上传→索引→检索→对话→引用链路，并完成 2 分钟演示视频。

垂直链路在代码中已接通：`extract.ts` 白名单 txt/md/pdf → `IndexingRunner` 五阶段与断点续跑 → `KnowledgeService.retrieve`（只 embed + 向量库）→ `ChatService.stream` 的 `collectCitations` / `assembleRagPrompt` → SSE `message.citations` → `CitationList`。前端有知识库详情「检索测试（不经过 LLM）」和对话页单库挂载。本会话抽测 39 条相关单测通过（knowledge / pipeline / chat.service / message-parts / chat-event-reducer / gen-tests verify）。

评测报告 `scripts/rag-eval/reports/20260827-0941-baseline.md` 与代码默认值一致的项：`qwen3.5:2b`、`nomic-embed-text:latest`、`numCtx=8192`、`numPredict=2048`、`temperature=0.2`、`recursive` / `chunkSize=500` / `overlap=50`、`topK=5`、`scoreThreshold=0.3`、`promptVersion=rag-v1-delimiter-escaped`。不一致：报告 `vectorBackend=memory`（评测当时 `/health` 无 `DATABASE_URL`）；有库时 `health.vectorStore` 为 `postgres`。引用准确率 0.10 是因为指标只认正文 `[#n]`，与 API 下发 citations 不是同一件事。

15-D：真实目标 `servers/liangzui-ai-server/src/chat/context-window.ts` 已审核入库；`*.generated.spec.ts` 在 `.gitignore`；弱测试会被变异门禁淘汰。

非阻塞缺口：失败文档需调 `reindex`（调度器不自动重试 failed），前端无 reindex 按钮；对话 UI 只挂载一个知识库；E2E `e2e/chat.spec.ts` 仅为页面 smoke。`.plan/07` 若干联调项仍未勾。

### M3 · 工作流

#### CR-11 · 工作流后端微内核（决策门禁）

- **包含**：`08`；对应的 `15` 测试；`16` 的沙箱、资源耗尽和 SSRF 用例。
- **必须单独 CR**：不与前端画布合并。
- **决策**：按 ADR-D02 判据确认 LangGraph 或切换自建拓扑执行器。
- **Review 重点**：新增节点是否零内核改动、VariablePool、取消传播、WASM 沙箱。

已通过（2026-08-27）。采用 ADR-D02 的自建串行拓扑执行器；CR 修复补齐运行快照与日志查询、停止语义、分支出口和模板变量引用校验。数据库迁移、集成测试及 `pnpm ci:local` 均通过。

#### CR-12 · 工作流画布

- **包含**：`09`；所需 `14-B` 组件与 E2E。
- **Review 重点**：单一 CustomNode、变量选择器、保存态与运行态分离、30 节点性能。

已通过（2026-08-28）。CR 修复补齐草稿强制提交、运行与停止竞态、SSE 失败终态、分支连线同步、微内核元数据和模板变量高亮；定向测试、E2E 与 `pnpm ci:local` 均通过。

#### M3 集成 Review

- 拖拽建图、保存、运行、日志、高亮、单节点调试完整走通。
- 用新增第 9 个节点验证插件化，而不是只读代码判断。

集成 Review 已通过（2026-08-28）。Playwright 已覆盖 30 节点画布的拖拽、保存、SSE 日志与高亮；真实 NestJS API 链路已走通创建、校验、运行、运行记录、单节点调试和清理。另以进程内 `ninth-test` runner 动态注册并执行 `start → ninth-test → end`，输出为 `plugin:ok`，工作流执行内核无需修改。`pnpm ci:local` 全部通过。

### M4 · Agent 与 MCP

#### CR-13 · Agent 核心与文件工具

- **包含**：`10`；对应测试；`16` 的路径穿越、符号链接逃逸与审批绕过用例。
- **必须单独 CR**：先证明不接 MCP 的 Agent 核心稳定。
- **Review 重点**：工具裁剪依据 04 实测、durable inbox、maxSteps、三层安全。

#### CR-14 · MCP 集成（可选）

- **包含**：`11`。
- **Review 重点**：MCP 工具不能绕过既有权限和工具数量上限；子进程退出无泄漏。
- 时间不足可标记 `已搁置`，不阻塞 M5。

#### M4 集成 Review

- 真实模型下验证选工具、参数缺失、非法工具名、dangling tool call 等失败路径。
- Agent 重启后状态一致，文件写入必须经过审批。

集成 Review 已通过（2026-08-28）。用户确认手测完成。证据：内置五工具可走通；`grep` 走 `@vscode/ripgrep` 自带二进制；写入走审批；拒绝/超时本轮收尾不再连弹；只读仍暴露 `write`/`edit` 但权限 deny 并立即收尾；配 `DATABASE_URL` 后 durable inbox 落 Postgres，重启刷新为中断而非永久 running；MCP filesystem 可连接，5 内置 + 勾选 MCP 超 `maxToolCount=6` 时内置优先裁 MCP。

残留（不阻塞 M4）：停服后 `ps` 无 MCP 子进程未当面确认；非法工具名 / dangling / JSON 兜底以单测为主，真实模型未逐条观测；2B 仍可能先 `glob` 再反问；`mcp.json` 尚无 `env` 字段。联网天气 MCP 与本地 `datetime` 不在 M4 范围，按用户指令作为范围外扩展，通过后再进 CR-15。

#### CR-X1 · 通用 Agent 实用工具扩展

- **性质**：用户指定的 M4 后置扩展；M4 已通过，不回写原里程碑结论。
- **包含**：
  - 内置 `datetime`：返回指定 IANA 时区的当前日期、时间、星期与 UTC 偏移。
  - 内置 `calculate`：解析基础算术表达式，不使用 `eval` / `new Function`。
  - 内置 `generate_uuid`：一次生成 1～10 个 UUID v4。
  - 天气 MCP：示例配置固定 `@dangahagan/weather-mcp@1.25.6`，只暴露单调用即可完成常见查询的 `get_weather_summary`；通过 `toolFilter.inputParams` 将模型可见参数投影为 `city_name` 与 `units`，并强制 `city_name` 必填。
  - 基于当前用户输入的确定性工具选择：命中日期、计算、UUID 或天气意图时优先放入对应工具，再补文件工具；任何请求仍不得超过 `capabilities().maxToolCount = 6`。
- **不包含**：任意 URL `webfetch`、Shell/命令执行、长期记忆、提醒/定时任务、多个天气工具、多 MCP 并行编排、修改第三方 MCP server。
- **Review 重点**：工具选择不能依赖另一次 LLM 调用；普通文件任务不能回归；天气 MCP 默认按 `execute` 走审批；时间输出可测试且不依赖机器本地时区；计算器不存在代码执行或资源耗尽路径。
- **通过条件**：
  - 日期/时间支持有效 IANA 时区并明确拒绝非法时区。
  - 计算器支持括号及 `+ - * / % ^`，拒绝非有限结果、超长表达式和非算术字符。
  - UUID 输出全部通过 UUID schema，数量边界生效。
  - “北京天气”只需向模型暴露一个天气 MCP 工具且总数不超过 6；未连接天气 MCP 时 Agent 能文本说明不可用，不伪造实时天气。
  - 定向单测、contracts 测试、lint、typecheck、构建与安全扫描通过。
- **提交边界**：规划提交；contracts + 内置工具；意图选择 + Agent 接入；天气 MCP 示例；进度更新。

实现、自检与 CR 已完成（2026-08-28）。`datetime`、`calculate`、`generate_uuid` 已注册；实时天气意图会优先选择 `get_weather_summary`，知识类天气问题不被短路，未连接时由服务端确定性提示。天气 MCP 1.25.6 已真实完成 stdio 握手与北京查询，返回 22°C 等实时数据；复杂 schema 已投影为 `city_name`（必填）和 `units`。`pnpm ci:local` 全绿，共 563 条测试通过；CR 未发现阻塞项。后续接入面按 ADR-014 从独立 Agent 页迁到统一对话。

#### CR-Y1 · 天气 MCP 预置可发现性

- 启动时把 `mcp.json.example` 中缺失的 server 非破坏性补入本地 `mcp.json`，不覆盖用户已有配置。
- weather 默认关闭；设置页区分内置工具与远程 MCP 工具，并提示启用 stdio MCP 会运行第三方进程。
- **通过条件**：已有配置不被覆盖；老用户重启后能看到 weather；启用和勾选继续即时生效。

实现与自检完成（2026-08-28）。真实启动已把 weather 以 `enabled=false` 补入现有配置，原 filesystem command、路径、开关与过滤器保持不变；`GET /mcp/servers` 同时返回 weather 与 filesystem。

#### CR-Y2 · 统一服务端对话编排

- `POST /chat/sessions/:id/stream` 接入请求级 `fileAccess` 与实用工具意图路由，扩展统一 SSE 事件。
- 文件访问关闭时文件工具与 filesystem MCP 不可达；开启时要求工作区并复用 durable inbox、沙箱、权限和审批。
- 实用工具或文件轮次首批不检索知识库；普通问答保留现有真流式与 RAG citations。
- **通过条件**：任意一轮工具总数不超过 6；伪造文件 tool call 无法绕过开关；旧 Agent API 仍兼容。

实现与自检完成（2026-08-28）。Chat 请求、统一 SSE 与 durable inbox 已持久化 `fileAccess`；关闭时文件内置工具和 filesystem MCP 均不进入候选，执行面也拒绝伪造调用。真实 `/chat` 调用 datetime 与 calculate 成功，数据库迁移 `0006` 已验证。

#### CR-Y3 · 统一对话页面

- `/chat` 统一展示历史会话、工具卡、审批与引用；文件访问作为同一会话中默认关闭的逐轮开关。
- `/agent` 路由兼容重定向，导航不再提供独立“文件助手”入口。
- **通过条件**：同一会话可完成普通问答、实用工具、开启文件访问、关闭后恢复最小权限；刷新不自动开启文件访问。

实现与自检完成（2026-08-28）。统一 Chat store/reducer 已承载工具状态、warning 与审批；文件开关按 sessionId 隔离并默认关闭；旧 Agent 前端页面、store 与 stream API 已删除。

#### CR-Y4 · 双端收敛与验证

- 清理无引用的独立 Agent 前端代码，完成 Web/Tauri 路由、目录选择、设置页和重启恢复走查。
- 承接原 CR-15 的 `12-B` 双端体验收尾，完成后再进入 CR-16。

实现、自检与 CR 已完成（2026-08-28）。审查发现并修复了审批期间重复发送、审批恢复重复、活动工具恢复与轮询、停止生成误清审批、会话切换状态泄漏及迟到 SSE 覆盖新请求等竞态；最终复审无阻塞项。Playwright 的 chat/agent/mcp 用例通过，旧 `/agent/:id` 可重定向并完成写入审批；`pnpm ci:local` 全绿，共 571 条测试通过，Web、Tauri 前端与 NestJS 构建、Rust clippy、SAST/SCA 均通过。受本机 `AGENT_WORKSPACE_ROOTS` 配置限制，项目根目录真实文件读取返回“路径越出工作区”，未绕过或修改用户白名单；文件读写链路由安全单测与浏览器审批 E2E 覆盖。

### M5 · 双端与打包

#### CR-15 · 双端业务装配

- **状态**：由 CR-Y4 承接，不再作为独立批次执行。
- **包含**：`12-B`，在现有双端壳上补全平台能力与体验。
- **Review 重点**：两个壳只做装配、app-core 零平台 API、设置页与后端连接失败引导。

#### CR-16 · dmg 阶段一（交付门禁）

- **包含**：`13-A`，前端 dmg + 可配置外部后端；本机打包通过后补 `17-C` release workflow。
- **必须先验收**：这是承诺交付物，完成后项目已经可以收尾。

#### CR-17 · sidecar 阶段二（可选）

- **包含**：`13-B`。
- **时间盒**：最多 4 天；失败则记录 ADR/风险并退回已完成的 CR-16。
- 不允许为了 sidecar 推迟项目交付。

#### M5 集成 Review

- Web 与 dmg 的统一对话、知识库、工作流、设置页面表现一致。
- 安装、Gatekeeper、CSP、错误后端地址、退出残留进程全部走查。
- `17-C` 只在本机 dmg 已验证后接入，且 tag 构建成功。

### 收尾

#### CR-S1 · 评测实验与交付材料

- **包含**：`18-C` 至少 3 组单变量优化实验；README/演示/简历材料；`19` 状态与 `20` ADR 补齐。
- **Review 重点**：报告可复现、文档与实际命令一致、简历数据有报告出处。
- 不新增业务功能；发现缺陷回到对应模块修复并重新验证。

## 贯穿 plan 的子阶段归属

| Plan | 子阶段                                                                | 所属 CR                                        |
| ---- | --------------------------------------------------------------------- | ---------------------------------------------- |
| 12   | 12-A 平台接口与最小双端壳 / 12-B 能力补全与体验收尾                   | CR-02 / CR-15                                  |
| 13   | 13-A 外部后端 dmg / 13-B sidecar                                      | CR-16 / CR-17                                  |
| 14   | 14-A 令牌底座 / 14-B 业务组件增量                                     | CR-03 / 各前端功能 CR                          |
| 15   | 15-A 测试框架 / 15-B 基础设施辅助 / 15-C 功能测试 / 15-D LLM 测试生成 | CR-01 / CR-05～06 / CR-03 与各功能 CR / CR-10A |
| 16   | 16-A 扫描工具 / 16-B 功能攻击测试                                     | CR-04 / CR-08、10、11、13                      |
| 17   | 17-A CI 骨架 / 17-B 数据库集成 / 17-C release                         | CR-04 / CR-06 / CR-16                          |
| 18   | 18-A 可观测性 / 18-B 评测基线 / 18-C 优化实验                         | CR-07 / CR-09 / CR-S1                          |

`19` 与 `20` 不建立独立功能 CR：前者每个批次更新执行状态，后者在技术决策发生时立即追加。

## 每个里程碑的 DoD

### M0 · 工程底座（2 周）

对应 plan：`02` `03` `12-A` `14`（令牌层）`15`（框架）`16`（工具接入）`17`（骨架）

| 完成标志                                             |
| ---------------------------------------------------- |
| `pnpm ci:local` 一条命令跑通全部门禁                 |
| 契约层建立，现有 translate 接口已契约化              |
| 平台适配层接口定义完成，两套实现骨架就位             |
| 最小 Web/Tauri 壳都能挂载同一 app-core 根组件        |
| 主题令牌系统就位，`/dev/tokens` 页面可看             |
| Playwright 第一条主题 smoke 通过                     |
| 四条架构护栏（ESLint no-restricted-imports）实测生效 |
| CI 在 GitHub 上跑通                                  |
| Semgrep 自定义规则通过自测                           |

**M0 的价值不在于产出功能，在于让后面 13 周的开发不返工。** 忍住直接写功能的冲动。

### M1 · 模型能力与数据层（1.5 周）

对应 plan：`04` `05`

| 完成标志                                   |
| ------------------------------------------ |
| 模型基线报告已提交，六项测评都有数据       |
| `numCtx` / `numPredict` 已用实测值替换     |
| `capabilities()` 返回的数值都有报告出处    |
| Docker + pgvector 跑通，数据持久化验证通过 |
| Drizzle 迁移体系建立                       |
| `LlmGateway` 与 `FakeLlmGateway` 就位      |
| 现有 `MemoryVectorStore` 已替换为 pgvector |

**M1 结束时要做一次决策复盘**：基线数据是否推翻了原定的功能设计？如果 tool call 不可用，现在就要改 `10` 的方案，而不是等到 M4。

### M2 · RAG + 对话页（3 周）

对应 plan：`06` `07`

| 完成标志                          |
| --------------------------------- |
| 能上传 txt/md/pdf 并索引成功      |
| 五阶段流水线状态可见，失败可重试  |
| 检索测试面板能看到召回详情与分数  |
| 对话页流式输出流畅，长回答不卡    |
| 挂载知识库问答带引用溯源          |
| 提示词注入测试通过（模型不照做）  |
| XSS 测试通过                      |
| 首次 RAG 评测基线报告产出（`18`） |

**这是第一个可以给人看的成果。** M2 结束后录一段 2 分钟的演示视频，之后每个里程碑都录一段。

### M3 · 工作流引擎 + 画布（3.5 周）

对应 plan：`08` `09`

| 完成标志                                             |
| ---------------------------------------------------- |
| 8 个节点全部可用                                     |
| 内核代码里搜不到具体节点类型的分支                   |
| 新增第 9 个节点时 engine 目录零改动（git diff 证明） |
| code 节点沙箱逃逸测试全部失败（即防护有效）          |
| http 节点 SSRF 测试全部拦截                          |
| 变量选择器可用，节点删除后引用失效有提示             |
| 运行态高亮 + 执行日志完整                            |
| 单节点调试可用                                       |
| 30 个节点时画布仍流畅                                |

**这是技术含量最高的部分**，也最容易做超时。如果时间紧，砍节点数量（从 8 个减到 6 个），**不要砍插件化架构**——架构是这个模块的核心价值。

### M4 · Agent 工具 + MCP（3 周）

对应 plan：`10` `11`

| 完成标志                                                        |
| --------------------------------------------------------------- |
| 5 个文件工具可用                                                |
| 路径穿越 + 符号链接逃逸测试全部被拦                             |
| 审批流程可用，带内容 diff                                       |
| 只读/编辑两种模式生效                                           |
| durable inbox 生效（杀进程重启不丢消息、无 dangling tool call） |
| maxSteps 强制收尾生效                                           |
| tool call 解析兜底生效                                          |
| MCP server 能连接并合并工具（或明确记录为搁置）                 |

`11`（MCP）是全项目优先级最低的。时间紧就搁置，在 README 里标"规划中"。

### M5 · 双端 + 打包（1.5 周）

对应 plan：`12-B` `13`（阶段一）

| 完成标志                                 |
| ---------------------------------------- |
| Web 端与桌面端四个页面表现一致           |
| `packages/app-core` 里搜不到平台专有 API |
| dmg 构建成功，安装后可用                 |
| 后端未启动时显示引导页而非白屏           |
| 设置页可配置后端地址，「测试连接」可用   |
| Tauri devtools 无 CSP 报错               |
| README 写明 Gatekeeper 处理方法          |

`13` 阶段二（sidecar）是可选加分项，设 4 天硬时间盒。

### 收尾（1 周）

| 内容                                                |
| --------------------------------------------------- |
| 完成 3 组以上 RAG 优化实验并有对比报告              |
| README 完善：架构图、快速开始、功能对照表、已知限制 |
| 各 plan 的状态更新为已完成                          |
| ADR 补齐（`20`）                                    |
| 录制完整演示视频（5 分钟）                          |
| 整理简历描述                                        |

## 简历产出物对照

这是整个项目的最终目的。每个里程碑对应可写进简历的内容：

| 产出物                        | 来自    | 简历上怎么说                                                                               |
| ----------------------------- | ------- | ------------------------------------------------------------------------------------------ |
| 模型能力基线报告              | M1      | "对本地 2B 模型做了六维能力量化测评（工具调用/上下文/结构化输出等），据此确定功能设计边界" |
| pnpm + turbo monorepo（6 包） | M0      | "设计并落地 6 包 monorepo，用 ESLint 依赖护栏防止架构腐化"                                 |
| 契约驱动开发                  | M0      | "以 zod schema 为前后端唯一类型来源，消除接口漂移"                                         |
| RAG 五阶段流水线              | M2      | "实现 extract→clean→split→embed→index 五阶段索引流水线，支持分阶段重试"                    |
| RAG 量化评测 + 优化实验       | M2/收尾 | "建立 Recall@k/MRR/拒答正确率评测体系，通过实验将 Recall@3 提升 X%"                        |
| SSE 流式 + event reducer      | M2      | "实现 part 级增量渲染的流式对话，解决长回答的重渲染性能问题"                               |
| 微内核工作流引擎              | M3      | "设计插件化微内核工作流引擎，新增节点类型零内核改动"                                       |
| WASM 代码沙箱                 | M3      | "用 QuickJS/WASM 实现代码节点沙箱，通过逃逸与资源耗尽测试"                                 |
| Agent 三层安全                | M4      | "实现路径沙箱→权限规则→用户审批的三层防护，抵御路径穿越与符号链接逃逸"                     |
| durable inbox                 | M4      | "借鉴 opencode 的持久化收件箱设计，保证进程崩溃不丢消息"                                   |
| 一套代码双端                  | M5      | "通过平台适配层实现 Web 与 Tauri 桌面端共用业务代码"                                       |
| LLM 生成用例流水线            | 贯穿    | "构建 选目标→组装上下文→生成→变异测试验证→覆盖率门禁 的测试生成流水线"                     |
| 安全扫描体系                  | 贯穿    | "针对 AI 应用特有攻击面编写 11 条 Semgrep 自定义规则 + 16 条渗透测试用例"                  |
| CI 分层流水线                 | 贯穿    | "分层 CI（快速门禁→测试→安全→构建），SARIF 统一汇总到 Code Scanning"                       |

## 如果时间不够，砍什么

按砍的优先顺序（先砍上面的）：

| 顺序 | 砍什么                                                  | 影响                                                 |
| ---- | ------------------------------------------------------- | ---------------------------------------------------- |
| 1    | `13` 阶段二（sidecar）                                  | 无。阶段一已满足需求                                 |
| 2    | `11`（MCP）                                             | 少一个热点技术词                                     |
| 3    | 工作流节点从 8 个减到 6 个（砍 `code`、`http-request`） | 少了两个有技术含量的节点（沙箱、SSRF），可惜但可接受 |
| 4    | 混合检索、父子切分等 RAG 进阶                           | 评测报告里说明为未来优化方向                         |
| 5    | 四套主题减到两套                                        | 无实质影响                                           |
| 6    | E2E 从 5 条减到 2 条                                    | 可接受                                               |

**绝对不砍的**（砍了项目就失去价值）：

- 模型基线测评（`04`）
- 工作流的插件化架构（`08` 的微内核）
- Agent 的三层安全（`10`）
- RAG 评测体系（`18` 第二部分）
- 覆盖率门禁与安全扫描（`15` `16`）
- 契约层（`03`）

这些是"工程能力"的体现。功能少一点没关系，工程做扎实了才是差异化。

## 进度追踪

每周做一次：

1. 更新 `.plan/README.md` 的进度表
2. 记录本周遇到的技术决策 → 追加到 `20-adr-and-risks.md`
3. 如果某个 plan 的估算明显偏离，更新本文件的排期

不要写周报式的日志文档，那是给别人看的。ADR 和进度表就够了。
